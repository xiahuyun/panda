# Panda Mobile 架构记录

更新时间: 2026-03-12

## 记录规则

1. 每完成一个重要功能或里程碑后，若模块边界或依赖关系发生变化，必须更新本文件。
2. 重点记录以下内容：
   - 页面/组件/服务模块划分
   - 关键依赖关系
   - 新增或调整的设计决策
   - 对测试与维护性的影响

## 当前架构（MVP）

1. 页面层：`LoginPage`、`AffinityWallActivePage`
2. 数据层：`mock/affinity-profiles.mock.json` 为真源，脚本生成 `mock/affinity-profiles.mock.uts` 供运行时读取
3. 样式层：`tokens/panda.tokens.json` 为唯一真源
4. 运行时：`uni-app x + Vue 3 + UTS/.uvue`

## 架构变更记录

> 示例模板（复制后填写）  
> 日期: YYYY-MM-DD  
> 变更点: xxx  
> 原方案: xxx  
> 新方案: xxx  
> 影响评估: xxx  
> 回滚方案: xxx

日期: 2026-03-11  
变更点: 登录主流程从 demo 单页切分为业务路由与服务模块  
原方案:
1. 主要逻辑集中在 `pages/index/index.uvue` demo 页面。
2. 登录、缘分墙、聊天 mock 混合在同一页面和同一 mock 服务文件。
新方案:
1. 页面路由拆分：
   - `pages/auth/login.uvue`
   - `pages/affinity/active.uvue`
2. 服务模块拆分：
   - `services/mock/login-service.uts`（登录规则）
   - `services/session/session-store.uts`（内存会话）
   - `services/analytics/local-event-logger.uts`（本地埋点）
3. 组件能力补齐：
   - `components/TextInput.uvue` 新增 `disabled` 能力。
影响评估:
1. 架构从“单页 demo”演进为“按页面/服务分层”，更符合后续迭代与测试要求。
2. 旧 demo 页 `pages/index/index.uvue` 仍保留，避免一次性大删引入回归风险。
3. 业务主路径已独立，可在 D2/D3 继续增量演进。
回滚方案:
1. 若新路由出现阻塞，可临时把 `pages.json` 启动页回退到 `pages/index/index`。
2. 新增服务模块为增量引入，不影响旧 mock 服务文件，回滚成本低。

日期: 2026-03-11  
变更点: 缘分墙活跃态接入独立 mock service 与业务状态流  
原方案:
1. `pages/affinity/active.uvue` 为静态占位页面，无真实数据加载过程。
2. 缘分墙资料未形成独立服务边界。
新方案:
1. 新增 `services/mock/affinity-wall-service.uts`，封装随机资料与 loading 时长。
2. `pages/affinity/active.uvue` 采用状态驱动（`loading` -> `active`）并消费 service。
3. 资料展示结构拆分为：
   - `ProfileCard`（基础资料）
   - 页面内 `reason-card`（推荐理由）
   - `online-row`（在线状态）
影响评估:
1. 缘分墙页面已具备可持续迭代的数据与状态基础，后续可无缝接入真实后端。
2. service 分层更清晰，页面职责聚焦于状态与展示，便于测试和维护。
3. 与产品文档“随机资料 + loading 默认 600ms”约束保持一致。
回滚方案:
1. 若新状态流出现问题，可临时将 `active.uvue` 回退到静态占位实现。
2. `affinity-wall-service.uts` 为新增模块，回滚时可直接移除且不影响登录主流程。

日期: 2026-03-11  
变更点: 补齐本地埋点入口并形成测试清单文档化闭环  
原方案:
1. 应用级生命周期仅有 `console.log`，未输出标准化 `app_launch` 事件。
2. 手测流程依赖临时口头约定，缺少固定 checklist。
新方案:
1. `App.uvue` 在 `onLaunch` 中接入 `logLocalEvent("app_launch", { app_version })`。
2. 维持本地事件日志模块作为统一入口（`services/analytics/local-event-logger.uts`）。
3. 新增 `docs/product/manual-test-checklist.md` 作为回归测试基线。
影响评估:
1. 事件链路从页面级扩展到应用级，埋点覆盖更完整。
2. 测试流程可重复、可审计，降低后续迭代回归成本。
回滚方案:
1. 若需要临时关闭应用级埋点，可移除 `App.uvue` 中 `app_launch` 日志调用。
2. 手测清单文档为非运行时依赖，移除不会影响代码执行。

日期: 2026-03-11  
变更点: 测试清单状态化并补充实现偏差追踪  
原方案:
1. `manual-test-checklist.md` 仅为条目列表，未标注“自动确认/待验证”状态。
2. 资料源一致性偏差未在架构记录中显式追踪。
新方案:
1. 手测清单增加状态标注，区分可自动确认项与端上验证项。
2. 将“缘分墙资料源未读取 mock 文件”的偏差登记为待修正事项。
影响评估:
1. 测试执行更可控，可快速识别剩余阻塞项。
2. 架构与文档一致性问题可被持续跟踪，降低后续返工风险。
回滚方案:
1. 清单状态标注属于文档增量，不影响运行时，必要时可回退到纯列表版本。

日期: 2026-03-11  
变更点: 缘分墙资料源统一为 mock 文件真源 + 生成产物消费  
原方案:
1. `services/mock/affinity-wall-service.uts` 内部维护硬编码资料池。
2. 与文档声明的 `mock/affinity-profiles.mock.json` 真源存在偏差。
新方案:
1. 新增同步脚本 `scripts/sync-affinity-profiles-mock.mjs`，将 JSON 真源转换为 `mock/affinity-profiles.mock.uts`。
2. `affinity-wall-service.uts` 仅消费 `AFFINITY_PROFILES_MOCK`，不再内置数据。
3. npm 脚本新增 `mock:sync`，作为资料源变更后的标准同步入口。
影响评估:
1. 产品文档、数据源、运行时代码三者一致，降低后续维护歧义。
2. service 职责收敛为“随机选择 + loading 时长控制”，边界更清晰。
3. 新增一步生成流程，开发时需保证 JSON 变更后执行同步命令。
回滚方案:
1. 若生成链路异常，可临时恢复 service 内置资料池保证功能可用。
2. 同步脚本与生成文件均为增量文件，可独立回退。

日期: 2026-03-11  
变更点: 补充端上验证工具链约束与执行阻塞结论  
原方案:
1. 默认认为可在当前仓库直接拉起本地运行环境执行端上手测。
2. 手测清单仅记录“待验证”，未沉淀环境层阻塞证据。
新方案:
1. 在 `manual-test-checklist.md` 中记录实际执行命令与失败原因（`dev:h5` 脚本缺失、`uni` CLI 兼容性失败）。
2. 将端上验证前置条件显式化：需 HBuilderX 或已锁定版本的 uni-app x CLI。
影响评估:
1. 手测工作从“口头待办”变为“可复现的环境问题清单”，降低重复排障成本。
2. 当前架构不变，但发布前验证门禁更清晰，避免误判为功能已端上通过。
回滚方案:
1. 文档化约束为非运行时变更，可按需回退，不影响现有代码执行。

日期: 2026-03-11  
变更点: 端上验证工具链正式锁定为 HBuilderX  
原方案:
1. 端上验证工具链为候选态（HBuilderX 或锁定版本 CLI）。
2. CLI 方案在当前环境存在兼容性失败记录。
新方案:
1. MVP 阶段端上运行与手测统一使用 `HBuilderX`。
2. 手测清单新增 HBuilderX 执行步骤，CLI 失败记录保留为历史上下文。
影响评估:
1. 手测入口统一，减少环境分歧与重复排障。
2. 不影响现有页面/服务模块边界，仅影响测试执行路径。
回滚方案:
1. 若后续需要恢复 CLI 路径，需先补齐可用脚本与版本锁定，再更新文档约束。

日期: 2026-03-11  
变更点: 端上验证状态由“待验证”闭环为“已通过”  
原方案:
1. 登录流程第 2.5 条（错误弹窗期间全屏禁交互 + 返回键拦截）处于待端上验证状态。
新方案:
1. 在 HBuilderX 完成端上手测后，将第 2.5 条状态更新为通过。
2. 手测清单执行结果同步到过程文档，形成闭环记录。
影响评估:
1. D1-D3 主路径验证门禁已满足，后续可在此基线上继续迭代。
2. 架构模块边界无变化，本次属于验证状态闭环更新。
回滚方案:
1. 若后续端上回归发现问题，可将第 2.5 条回退为“不通过（附现象）”并按缺陷流程修复。

日期: 2026-03-11  
变更点: 登录成功跳转链路增加降级与错误兜底  
原方案:
1. 登录成功后仅调用 `uni.reLaunch("/pages/affinity/active")`。
2. 当运行环境路由能力异常时，存在未捕获 Promise 报错，且无用户可见反馈。
新方案:
1. 登录成功后改为 `uni.redirectTo("/pages/affinity/active")`。
2. `redirectTo` 失败自动降级到 `navigateTo`。
3. 二次失败时给出 toast 提示并输出 console 错误，便于排障。
影响评估:
1. 跳转链路从“单点调用”变为“主路径 + 降级路径 + 失败提示”，健壮性更高。
2. 不改变页面模块边界，仅调整登录页导航实现细节。
回滚方案:
1. 若后续确认 `reLaunch` 在目标平台完全稳定，可回退为单 API 实现以简化代码。

日期: 2026-03-11  
变更点: 页面/组件脚本范式统一为 script setup + 自动门禁  
原方案:
1. `pages/auth/login.uvue`、`pages/affinity/active.uvue` 使用选项式 `export default`。
2. 工程未对脚本范式做自动检查，后续可能混入多种写法。
新方案:
1. 业务页面统一改为 `<script setup lang=\"uts\">` 组合式写法。
2. 新增 `scripts/script-setup-scan.sh`，并接入 `ui:check`，阻止非 `<script setup>` 页面/组件提交。
3. `App.uvue` 保持选项式写法，作为框架能力限制的显式例外。
影响评估:
1. 页面/组件脚本范式统一，维护和 code review 成本下降。
2. 通过自动扫描把规范从“约定”升级为“可执行门禁”。
回滚方案:
1. 若扫描门禁影响紧急修复节奏，可临时从 `ui:check` 移除，但需保留文档说明和补测计划。

日期: 2026-03-11  
变更点: 登录输入层从“超长截断”改为“非法态保留”  
原方案:
1. `pages/auth/login.uvue` 中 `filterDigits` 同时执行“过滤非数字 + 最大长度截断”。
2. 超长输入会被截断为合法长度，导致提交禁用规则被绕过。
新方案:
1. `filterDigits` 仅过滤非数字，不再做最大长度截断。
2. 合法性由 `isLoginFormValid`（长度严格等于 11/6）统一判定，非法输入保持非法态。
影响评估:
1. 输入层行为与产品规则一致：非 11 位手机号、非 6 位验证码均禁用提交。
2. 交互可解释性更强，避免“用户看到超长值但内部已被截断”的认知偏差。
回滚方案:
1. 若后续策略改为“限制输入上限”，可恢复 `maxlength`/截断方案，但需同步更新 PRD 与验收清单。

日期: 2026-03-11  
变更点: script setup 门禁脚本增加 `rg` 缺失兼容层  
原方案:
1. `scripts/script-setup-scan.sh` 仅调用 `rg` 做匹配检查。
2. 在未安装 `rg` 的 CI 环境中，脚本会逐文件误报“不使用 script setup”。
新方案:
1. 抽象 `match_script_setup`：`rg` 可用时使用 `rg`，否则回退到 `grep -E`。
2. 门禁策略保持不变，仅增强执行环境兼容性。
影响评估:
1. CI 对工具链前置依赖降低，门禁结果更可信。
2. 本地与 CI 的脚本行为一致性提升，减少“本地通过/CI失败”的噪声。
回滚方案:
1. 若后续统一在 CI 安装 `rg`，可回退到单实现；当前双实现更稳妥。

日期: 2026-03-11  
变更点: D4 阶段策略锁定为“稳定性收口，不扩架构边界”  
原方案:
1. D4 方向未显式落盘，存在被临时需求拉动扩面的风险。
新方案:
1. D4 明确采用 A 路线：仅做稳定性/体验优化，不新增页面、不新增业务流。
2. 继续沿用当前页面与服务分层，不调整模块边界。
影响评估:
1. 可减少变更面，提升回归效率与内测可控性。
2. 架构保持稳定，有利于快速封版。
回滚方案:
1. 若需扩展功能，先更新 PRD 与 `ui-schema.stitch.json`，再进入代码实现。

日期: 2026-03-11  
变更点: D4-A 稳定性回归通过，架构状态确认  
原方案:
1. D4-A 回归结果尚未沉淀到架构记录，状态仅存在于执行沟通中。
新方案:
1. 确认 D4-A 回归通过，当前页面/服务分层保持不变。
2. 将本轮结果回填至手测清单与过程记录，形成“实现-验证-文档”闭环。
影响评估:
1. 架构边界稳定，无新增耦合与扩面风险。
2. 可直接进入封版与内测准备阶段。
回滚方案:
1. 若后续回归出现问题，按缺陷定位在既有模块内最小修复，不调整总体架构。

日期: 2026-03-11  
变更点: 封版与内测准备流程文档化（D4 P2）  
原方案:
1. 封版与内测步骤分散在口头沟通与多份文档中，缺少统一执行入口。
新方案:
1. 新增 `release-prep-checklist.md` 作为封版与内测单一执行清单。
2. `development-checklist.md` 增加 D4 P2 入口，明确回填字段与执行顺序。
影响评估:
1. 封版流程可复用、可审计，降低遗漏风险。
2. 不改变运行时代码与模块边界，仅强化发布流程治理。
回滚方案:
1. 如后续发布流程重构，可将清单迁移到新文档，但需保持单一权威入口。

日期: 2026-03-11  
变更点: 业务范围从“登录+缘分墙展示”扩展到“缘分墙动作闭环（D5 P0）”  
原方案:
1. 缘分墙仅展示资料，不包含 `再等等/感兴趣` 动作状态机。
2. 无消息栏页面路由定义，仅保留后续扩展位。
新方案:
1. 在架构层新增动作域：`wait` / `interested`，并明确同日单推荐约束（动作后不刷新下一张）。
2. 新增消息栏占位页作为最小路由终点：`/pages/message/index`（仅占位，不含真实聊天能力）。
3. 保持现有分层（page / service / mock / analytics）不变，在服务层增量扩展动作决策逻辑。
影响评估:
1. 在不接入真实后端前提下补齐业务闭环，便于后续替换真实匹配服务。
2. 变更面集中在缘分墙页面与 mock service，风险可控。
回滚方案:
1. 若动作闭环实现引发稳定性问题，可先保留按钮展示并临时降级为 toast 占位，不改路由主干。

日期: 2026-03-11  
变更点: D5 P0 规则细化为单一实现口径（7 点澄清）  
原方案:
1. D5 文档虽已扩 scope，但匹配规则/动作锁定/返回路径/埋点命名仍存在可解释空间。
新方案:
1. 匹配规则固定化：`profileId=="p_001"` -> `matched=true`，其余为 `false`。
2. 动作状态机固定化：当天首个动作即锁定，不允许改选。
3. 未匹配反馈固定化：toast 文案统一为 `今日匹配已用完，请明日再来`。
4. 时间边界固定化：设备本地自然日（00:00）。
5. 消息占位页返回固定化：按当前 `dateKey` 回到缘分墙应展示卡片（立即重算）。
6. 埋点命名固定化：新增 D5 动作事件清单。
影响评估:
1. D5 P1 进入“单一口径实现”状态，减少二次返工与分支解释成本。
2. 规则对 mock 池字段有显式依赖，需保证 `profileId` 稳定。
回滚方案:
1. 若后续切换真实匹配服务，保留事件与状态机骨架，仅替换匹配决策实现。

日期: 2026-03-11  
变更点: D5 P0.2 规则补充落盘（同日同对象 + 锁定存储边界 + 00:00 自动重置）  
原方案:
1. D5 P0.1 已锁定动作规则，但“同日重登对象稳定性”“动作锁定生命周期”“00:00 重置触发方式”仍未完全显式化。
新方案:
1. 推荐对象策略固定为“同一自然日同一用户稳定选择同一对象（含重新登录）”。
2. 动作锁定策略固定为“仅内存，App 进程被杀后不保留”。
3. 锁定后重复点击动作固定为“静默忽略，不弹 toast”。
4. 日期边界策略固定为“设备本地时间 00:00 自动重置当日推荐与动作锁定”。
5. `再等等` 文案固定为 `已为你安排明日推荐`（MVP 不做文案分叉）。
6. 工程治理策略固定为“`tech-stack.md` 待锁定项在 D5 功能完成后统一锁定”。
影响评估:
1. D5 P1 实现可按“推荐对象稳定策略”和“动作锁定内存策略”分别落地，避免语义耦合。
2. 用户可在 App 被杀后同日重新选择动作，同时保持推荐对象不漂移，体验一致且实现可控。
回滚方案:
1. 若后续产品改为“被杀后保留动作锁定”，可仅替换动作锁定存储层（如切到本地持久层），推荐对象稳定策略保持不变。

日期: 2026-03-12  
变更点: D5 P0.3 实现关键口径补齐（stable_pick_unseen 键来源 + 00:00 即时换卡 + conversationId 公式）  
原方案:
1. 已有 `stable_pick_unseen(userId, dateKey, cycleHistory)` 与 00:00 自动重置描述，但未明确 `userId` 具体来源与“页面停留到点是否立即换卡”。
2. `matched=true` 后仅有 `conversationId` 示例值，缺少确定性生成公式。
新方案:
1. `stable_pick_unseen` 的 `userId` 固定读取 `sessionUser.userId`，登录 mock 约定用户标识可稳定映射手机号。
2. 00:00 重置固定为“页面停留中到点立即重置并换卡”，不依赖 `onShow`。
3. `conversationId` 固定规则为 `profileId + "_" + dateKey`，并同步到导航与埋点文档。
影响评估:
1. 推荐对象、动作状态与会话标识都具备确定性，可复现性和可测试性提升。
2. D5 P1 实现阶段减少“时序触发点”和“ID 生成策略”的解释分歧。
回滚方案:
1. 若后续需改会话标识策略，可在不改 UI 路由的前提下仅替换 `conversationId` 生成函数并同步埋点字段。

日期: 2026-03-12  
变更点: D5 P0.4 推荐策略从“日维度稳定”升级为“用户历史去重”  
原方案:
1. 推荐策略仅保证“同日稳定”，未显式约束“历史推荐不重复”。
新方案:
1. 推荐策略升级为“同日稳定 + 历史去重”。
2. 新增用户级推荐历史约束：同一用户历史已推荐对象后续不再推荐。
3. 00:00 到点换卡目标明确为“历史未推荐对象”。
4. 动作输入 `userId` 示例统一为 `mock_user_${phone}`，与登录 mock 用户标识风格一致。
影响评估:
1. 推荐逻辑从“时间片稳定”提升为“时间片稳定 + 历史去重”，更符合产品目标。
2. 需要在实现侧新增用户推荐历史存储与读取能力（可与会话存储解耦）。
回滚方案:
1. 若后续临时回退到日维度策略，可保留历史结构不删，仅切换选择器实现。

日期: 2026-03-12  
变更点: D5 P0.5 推荐历史循环与清理边界锁定  
原方案:
1. 推荐去重已明确，但“未推荐池耗尽后的兜底”与“历史清理触发条件”仍未写死。
新方案:
1. 未推荐池耗尽后当日保持当前卡片；下一天 00:00 重置推荐历史并重新循环。
2. 推荐历史仅在“卸载/清除应用数据”时清空，常规重启不清空。
影响评估:
1. 推荐策略完整闭环，D5 实现不再需要临时兜底分支。
2. 持久层策略与产品行为一致，减少跨天回归歧义。
回滚方案:
1. 若后续改为长期不重置历史，可仅调整“00:00 重置”策略，保留数据结构不变。

日期: 2026-03-12  
变更点: D5 P0.6 双历史模型语义收敛（cycleHistory + lifetimeHistory）  
原方案:
1. 文档同时出现“次日 00:00 重置历史”和“仅卸载/清数据时清空历史”，存在术语歧义。
新方案:
1. `cycleHistory`：用于推荐去重，未推荐池耗尽后于次日 00:00 重置并进入新循环。
2. `lifetimeHistory`：用于长期留痕，仅在卸载/清除应用数据时清空。
3. 选择器明确为 `stable_pick_unseen(userId, dateKey, cycleHistory)`。
影响评估:
1. 推荐规则语义闭环，避免实现阶段误把“重置”与“清空”当同一动作。
2. 便于后续扩展推荐策略时保持向后兼容（保留长期历史）。
回滚方案:
1. 若后续取消双历史，可将 `lifetimeHistory` 下沉为仅日志用途，不影响 `cycleHistory` 主流程。

日期: 2026-03-12  
变更点: D5 P0.7 规则补充收敛（边沿埋点 + 新循环首卡约束 + 消息页跨日边界）  
原方案:
1. 文档未显式写明 `stable_pick_unseen` 是否允许实现自定义算法。
2. `login_submit_blocked` 在多份文档中的触发口径未统一为“合法 -> 非法”边沿触发。
3. 新循环首卡与上一日末卡关系未被硬性约束。
4. 消息占位页跨 00:00 行为未显式声明（可能被误实现为与缘分墙同策略）。
5. 非 MVP 聊天组件处理口径存在“移除”与“保留未接入”并存。
新方案:
1. 明确 `stable_pick_unseen` 算法实现可自定，但行为必须满足约束。
2. 明确 `cycleHistory` 与 `lifetimeHistory` 均按 `userId` 本地持久化。
3. 明确 `login_submit_blocked` 采用“合法 -> 非法”边沿触发，持续非法不重复触发。
4. 明确新循环首个推荐对象优先不等于上一自然日最后展示对象（无可选候选可降级重复）。
5. 明确消息占位页在 MVP 不做跨 00:00 特殊处理。
6. 明确非 MVP 聊天组件保留文件但不接入主流程。
影响评估:
1. D5 P1 实现边界更清晰，减少“同词不同义”导致的返工。
2. 推荐状态机需要额外保留“昨日末卡”信息用于次日首卡约束校验。
3. 消息占位页可继续保持极简占位实现，降低本轮改动面。
回滚方案:
1. 若后续产品改为“消息页跨日也即时刷新”，可只在消息页新增跨日策略，不影响缘分墙既有约束。

日期: 2026-03-12  
变更点: D5 P0.8 边界定义补充（首屏埋点触发 + 昨日末卡语义 + 无候选降级 + 返回重算）  
原方案:
1. `login_submit_blocked` 是否在首次进入初始非法态触发未显式写死。
2. “上一自然日最后展示对象”在实现上可能被理解为“用户最后看到的卡”，语义有歧义。
3. 新循环首卡“优先不等于昨日末卡”未定义“无可选候选”时的降级策略。
4. 消息占位页跨日虽不处理特殊逻辑，但返回缘分墙是否立即按当前日期重算未显式固化。
5. “非 MVP 聊天组件保留但不接入”缺少可执行工程门槛。
6. `loading=600ms` 在验收中是否刚性阈值未显式写死。
新方案:
1. `login_submit_blocked` 固定为“合法 -> 非法”边沿触发，首次进入初始非法态不触发。
2. “上一自然日最后展示对象”固定为系统最后一次成功分配推荐对象（last assigned）。
3. 若无候选可满足“首卡 != 昨日末卡”，允许降级重复上一日末卡。
4. 消息占位页返回缘分墙时，按当前 `dateKey` 立即重算并展示应展示卡片。
5. 非 MVP 聊天组件接入门槛固定为：主流程页面禁止 import，主流程路由禁止跳转。
6. `loading=600ms` 固定为默认可调参数，不作为硬性验收阈值。
7. 历史条目中涉及本节主题的表述已统一替换为本节口径。
影响评估:
1. 推荐状态机在边界场景（无候选/跨页返回）具备确定行为，测试与实现口径一致。
2. 埋点触发语义更稳定，避免首屏噪声事件干扰统计。
3. 非 MVP 组件保留策略可执行，减少“文件存在即被误接入”的风险。
回滚方案:
1. 若后续产品改为严格不允许重复昨日末卡，需在数据池规模与候选策略上新增硬约束，再调整降级策略。

日期: 2026-03-12  
变更点: D5 P1 动作闭环与消息占位页架构落地  
原方案:
1. D5 P0 阶段仅完成文档与规则基线，动作状态机与消息占位页路由尚未落地代码。
2. 推荐策略在实现层仍处于待接入状态，无法验证“同日稳定 + 历史去重 + 跨日重算”闭环。
新方案:
1. 页面层落地：
   - `pages/affinity/active.uvue` 新增动作区、锁定态展示、匹配结果分支、消息占位页导航与返回重算。
   - 新增 `pages/message/index.uvue` 作为消息占位页终点，固定文案并记录页面曝光事件。
2. 服务层落地：
   - `services/mock/affinity-wall-service.uts` 接入用户级推荐状态（按 `userId` 持久化）。
   - 推荐策略固定为“同日稳定 + 历史去重”；推荐池耗尽当日保持、次日重置循环；新循环首卡优先避开昨日末卡（无候选可降级）。
   - 动作锁定保持内存态，满足“进程被杀后锁定不保留”。
3. 标识与事件口径落地：
   - 登录 mock `userId` 固定映射手机号（`mock_user_${phone}`）。
   - `conversationId` 生成规则固定为 `profileId + "_" + dateKey`。
   - `login_submit_blocked` 固定为“合法 -> 非法”边沿触发。
影响评估:
1. D5 从“文档先行”转为“代码可运行闭环”，页面、服务、埋点三层约束一致。
2. 推荐状态机复杂度上升，但状态边界已拆分为“持久推荐状态”和“内存动作锁定”，便于定位问题。
3. 消息能力仍保持占位页，不引入真实聊天链路，符合 MVP 控制面。
回滚方案:
1. 若 D5 回归出现阻塞，可先保留动作区与消息页路由，临时降级推荐策略为“同日稳定”并保留历史结构，避免二次重构。

日期: 2026-03-12  
变更点: D5 P2 端上回归状态闭环（验证条目全部通过）  
原方案:
1. D5 P1 已完成代码落地，但仍保留“待端上验证”的执行状态。
新方案:
1. D5 P1 相关端上验证条目全部通过（HBuilderX）。
2. 手测清单状态统一回填为“已端上验证通过（HBuilderX，2026-03-12）”。
3. 架构状态由“已实现待端上回归”更新为“已实现且已端上回归通过”。
影响评估:
1. D5 MVP 主流程从实现到验证形成闭环，后续可直接进入封版准备。
2. 当前页面/服务边界保持稳定，无需新增架构调整。
回滚方案:
1. 若后续出现端上回归问题，可在不改变分层边界的前提下按缺陷点最小修复，并将条目状态回退为“不通过（附现象）”。
