# Panda Mobile MVP 决策记录 (2026-03-11)

## 1. 决策结论（已确认）

1. 需要将 `ui-schema` 收敛到 MVP 范围。
2. 路由结构可以拆分为真实页面路由，不再仅用 `pages/index/index` 演示页。
3. mock 验证码规则：
   - 验证码长度必须为 6 位
   - mock 测试码固定为 `123456`
4. MVP 登录失败仅处理一种错误：`验证码错误`，弹窗提示后 **3 秒自动关闭**。
5. MVP 不使用登录鉴权 token，不持久化登录状态；登录成功信息只保存在内存。
6. 缘分墙活跃态数据字段需要补全（见下文）。
7. UI 状态规则放宽：不强制所有页面都覆盖 `loading/empty/error/content` 四态，按页面类型定义。
8. 输入不合法时前端拦截并禁用提交，不触发 mock 校验。
9. 隐私政策/用户协议点击行为在 MVP 阶段先用 toast 占位，且文案区分。
10. 需要埋点方案，先本地打印事件，不接远端。
11. 缘分墙本轮仅做成功路径，不做错误分支。
12. 缘分墙 `membership_entry` 灰态展示，不可点。
13. `login_submit_blocked` 在输入非法时触发。
14. `login_submit_blocked` 不去重，输入非法即触发。
15. 错误弹窗 3 秒期间全屏完全禁交互（含输入、返回与任意页面点击）。
16. 协议/隐私 toast 触发需节流，1500ms 内不重复弹出，且两者各自独立计时。
17. 登录与缘分墙 `loading` 当前默认最小时长为 600ms（可按体验调整）。
18. 登录手机号仅校验 11 位数字，不限制号段。
19. 缘分墙资料从本地 mock 池随机选择 1 条，mock 池最小 3 条。
20. mock 池文件路径固定为 `panda-mobile/mock/affinity-profiles.mock.json`。
21. 会员入口以灰态呈现，不可点击。
22. 非 MVP 聊天组件将移出主分支，避免无用代码干扰。

## 2. 缘分墙活跃态字段（MVP）

```json
{
  "id": "p_001",
  "name": "林静宜",
  "avatarUrl": "/static/logo.png",
  "meta": "26岁 · 165cm · 上海",
  "bio": "热爱生活中的小确幸...",
  "reason": "你们在兴趣和交流节奏上较匹配",
  "isOnline": true
}
```

## 3. 下一步执行清单（未开始写代码）

1. 更新路由为 `LoginPage` -> `AffinityWallActivePage`。
2. 按 6 位规则 + 固定测试码 `123456` 实现 mock 校验，并在前端先做格式拦截。
3. 登录失败弹窗实现自动关闭（3 秒）且期间锁定交互。
4. 登录成功信息写入内存会话（不持久化）。
5. 缘分墙仅保留成功态实现，会员入口灰态展示且不可点击。
6. 接入 `analytics-events.md` 定义的最小埋点（本地打印），并按输入变非法触发 `login_submit_blocked`。
7. 协议/隐私入口分别接入占位 toast 文案，并增加 1500ms 各自独立节流。
8. 登录与缘分墙 `loading` 当前默认最小时长设置为 600ms（后续可按体验调整）。
9. 建立并使用 `panda-mobile/mock/affinity-profiles.mock.json`（至少 3 条）作为随机资料池。
10. 移除主分支中的非 MVP 聊天组件与聊天流代码。
